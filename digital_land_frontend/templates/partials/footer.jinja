{% set github_organisation  = github_organisation | default('digital-land') %}
{% set github_repository  = github_repository | default('digital-land') %}
{% set github_url = github_url | default('https://github.com/' + github_organisation + '/' + github_repository + '/') %}
{% set code_url = code_url | default(github_url) %}
{% set data_url = data_url | default(github_url) %}
{% set licence_url = licence_url | default(github_url + 'blob/master/LICENSE') %}
{% set footer_container_classes = footer_container_classes | default("")  %}

{{ govukFooter({
    "containerClasses": footer_container_classes,
    "meta": {
        "items": [
            {
                "href": "/cookies",
                "text": "Cookies"
            },
            {
                "href": "/accessibility-statement",
                "text": "Accessibility statement"
            },
            {
                "href": "/design-system",
                "text": "Design system"
            }
        ],
        "html": 'The <a class="govuk-footer__link" href="' + code_url + '">software</a> and <a class="govuk-footer__link" href="' + data_url + '">data</a> used to build these pages is <a class="govuk-footer__link" href="' + licence_url + '">open source</a>.'
    }
}) }}